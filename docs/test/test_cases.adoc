= Test Cases: {project-name}
include::../_includes/default-attributes.inc.adoc[]
// Platzhalter für weitere Dokumenten-Attribute

== Zweck des Dokuments
Dieses Dokument beschreibt die Testfälle für die Habit Tracker Anwendung. Es umfasst Akzeptanztests, die sicherstellen, dass die Anwendung den Anforderungen entspricht und korrekt funktioniert.
Es dient als Referenz für Tester, Entwickler und Stakeholder, um die Qualität der Software zu gewährleisten.

== Testkonzept
Wir haben in unserem Programm 3 verschiedene Testarten implementiert:
- **Unit Tests**: Diese Tests überprüfen einzelne Funktionen und Komponenten der Anwendung isoliert, um sicherzustellen, dass sie wie erwartet funktionieren, unabhängig von anderen Teilen des Systems.
- **Integration Tests**: Diese Tests überprüfen das Zusammenspiel mehrerer Komponenten oder Module, um sicherzustellen, dass sie zusammen korrekt arbeiten.
- **Systemtests**: Diese Tests werden von Testern durchgeführt, um die Benutzeroberfläche und die Benutzererfahrung zu überprüfen. Sie stellen sicher, dass die Anwendung aus Sicht des Endbenutzers funktioniert.

== Testtechnologien
Für die verschiedenen Testarten verwenden wir unterschiedliche Technologien:
- **Unit Tests Frontend**: Wir verwenden Jest als Testframework für unsere Unit Tests zusammen mit der React Testing Library. Jest ermöglicht es uns, einzelne Funktionen und Komponenten der Anwendung zu testen und sicherzustellen, dass sie korrekt funktionieren. Es können Mock-Daten und -Funktionen verwendet werden, um Abhängigkeiten zu isolieren.
- **Integration Tests**: Auch für die Integration Tests verwenden wir Jest in Kombination mit der React Testing Library. Diese Kombination ermöglicht es uns, das Zusammenspiel mehrerer Komponenten zu testen und sicherzustellen, dass sie zusammen korrekt arbeiten.
- **Systemtests**: Die Systemtests werden von Testern durchgeführt, die die Anwendung in verschiedenen Szenarien verwenden, um sicherzustellen, dass sie den Anforderungen entspricht und eine gute Benutzererfahrung bietet.

== Dokumentation der Tests
Die einzelnen Testfälle werden in den User-Stories im Github dokumentiert und auch in diesem Dokument festgehalten. Dort sind die Akzeptanzkriterien und die erwarteten Ergebnisse festgehalten. 

== Tests

=== Frontend Unit Tests 
Wir haben Unit Tests für die folgenden Komponenten implementiert:
* Navbar-Komponente:
** rendert korrekt die Navigations-Links
** prüft aktive Zustände für Übersicht und Hinzufügen
** verlinkt auf die richtigen Pfade
** zeigt Logout-Button und ruft onLogout auf

* Begleiter-Komponente:
** zeigt je nach Stimmung (glücklich/traurig) das passende Bild
** klick auf Begleiter ruft onClick-Callback auf

* BegleiterModal-Komponente:
** rendert alle verfügbaren Begleiter (Katze, Hund, Wurm, Hamster)
** Auswahl markiert Begleiter visuell und wechselbar
** Speichern ruft setAnimal mit Token/Typ/Stimmung auf
** Speichern ohne Auswahl schließt ohne API-Call
** Close-Button schließt ohne zu speichern
** API-Fehler werden geloggt, onSelect wird nicht aufgerufen

* HabitCard-Komponente:
** zeigt Habit-Name, Beschreibung und Streak
** Kartenklick triggert onClick
** Löschen-Button ruft onDelete mit ID auf
** Checkbox toggelt onCheck mit ID und Zustand
** Löschen klickt ohne Card-Click-Propagation

* HabitGrid-Komponente:
** zeigt Kopfzeile „Bereits heute erledigt“ an
** trennt erledigte von offenen Habits
** Klick auf Checkbox ruft onCheck auf
** Löschen-Button ruft onDelete auf
** rendert auch bei leerer Liste stabil
** zeigt Habit-Details und Streak-Informationen

* CalendarModal-Komponente:
** zeigt Titel mit ausgewähltem Datum
** listet erledigte und nicht erledigte Habits
** zeigt leere Zustände bei fehlenden Einträgen
** Close-Button ruft setOpenModal(false) auf

* Calender-Komponente:
** rendert Monats-Header und Wochentage
** rendert Tage des Monats
** Navigation zu vorherigem/nächstem Monat über Buttons
** funktioniert mit leerem Habit-Array

* HabitChangeModal-Komponente:
** Speichern mit geändertem Namen ruft edit(id, {name, description}) auf
** Speichern mit geänderter Beschreibung ruft edit korrekt auf
** rendert initiale Habit-Daten
** Speichert sowohl Änderungen bei Name und Beschreibung, wenn beide geändert wurden

* HabitCreateContent-Komponente:
** Submit-Button deaktiviert ohne Name, aktiviert mit Name
** Submit ruft onSubmit(name, description) auf
** verhindert Submit, wenn der Name nur aus Leerzeilen besteht
** erlaubt leere Beschreibung (übermittelt leeren String)
** loggt Fehler bei fehlgeschlagenem Submit

* HabitInfoModal-Komponente:
** zeigt Habit-Namen und optionale Beschreibung
** zeigt Streak, Start-Datum, letztes Erledigt-Datum mit Fallbacks
** zeigt Abschlussraten und 30-Tage-Statistik

* Login-Komponente:
** Submit ruft onSubmit mit E-Mail und Passwort auf
** Register-Button ruft switchToRegister auf

* Signup-Komponente:
** Submit ruft onSubmit mit E-Mail und Passwort auf
** Login-Button ruft switchToLogin auf

== Systemtests / Akzeptanztests
Die folgenden Akzeptanztests wurden definiert, um die Hauptfunktionen der Habit Tracker Anwendung zu überprüfen:
=== Akzeptanztest AT-01
Anzeige der geplanten Habits des aktuellen Tages

### Ziel des Tests:

Überprüfung, ob eine Übersicht aller für den aktuellen Tag geplanten Habits angezeigt wird

### Voraussetzung (Eingabe / Systemzustand):

Nutzer ist eingeloggt
Für den aktuellen Tag sind Habits geplant

### Aktion:

Nutzer öffnet die Startseite der App

### Erwartetes Ergebnis (Ausgabe):

Eine Liste aller für den aktuellen Tag geplanten Habits wird angezeigt


### Akzeptanztest AT-02
Visuelle Unterscheidung erledigter und offener Habits

### Ziel des Tests:
Überprüfung der visuellen Unterscheidbarkeit von erledigten und offenen Gewohnheiten

### Voraussetzung (Eingabe / Systemzustand):

Nutzer ist eingeloggt
Es existieren erledigte und offene Habits für den aktuellen Tag

### Aktion:
Nutzer betrachtet die Habit-Übersicht

### Erwartetes Ergebnis (Ausgabe):
Erledigte Gewohnheiten sind visuell eindeutig von offenen unterscheidbar

### Testergebnis:

- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-03
Sichtbarkeit der Habit-Übersicht nach Login und über Menü

### Ziel des Tests:
Überprüfung der Erreichbarkeit der Habit-Übersicht

### Voraussetzung (Eingabe / Systemzustand):

Website der App ist erreichbar und per Browser geöffnet

### Aktion:
Nutzer betrachtet die Startseite

### Erwartetes Ergebnis (Ausgabe):
Die Habit-Übersicht ist sichtbar

### Zusätzliche Aktion:
Nutzer navigiert über den Menüpunkt zur Übersicht

### Erwartetes Ergebnis:
Die Übersicht kann über den Menüpunkt aufgerufen werden

### Testergebnis:

- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-04
Sortierung der Gewohnheiten nach Startdatum

### Ziel des Tests:
Überprüfung der Sortierreihenfolge der angezeigten Gewohnheiten

### Voraussetzung (Eingabe / Systemzustand):

Es existieren mehrere Gewohnheiten mit unterschiedlichen Startdaten

### Aktion:
Nutzer betrachtet die Habit-Übersicht

### Erwartetes Ergebnis (Ausgabe):
Die Gewohnheiten sind nach Startdatum sortiert

### Testergebnis:

- [ ] bestanden
- [ ] nicht bestanden

---

## Userstory : B030

### Akzeptanztest AT-01
Öffnen eines bestehenden Habits zur Bearbeitung.

### Ziel des Tests:
Überprüfung, ob ein bestehender Habit zur bearbeitung geöffnet werden kann.

### Vorraussetzung 
Nutzer ist eingeloggt
Es existiert mindestens ein Habit

### Aktion:
Nutzer wählt ein bestehendes Habit aus

### Erwartetes Ergebnis (Ausgabe)
Eine Bearbeitungsansicht für den ausgewählten Habit wird angezeigt

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden


### Akzeptanztest AT-02
Änderung von Habit-Eigenschaften

### Ziel des Tests:
Überprüfung, ob die Eigenschaften eines Habits geändert werden können.

### Vorraussetzung 
Bearbeitungsansicht eines Habits ist geöffnet.

### Aktion:
Nutzer ändert mindestens eine Eigenschaft (z.B. Titel, Zeit, Dauer oder Häufigkeit).
Nutzer speichert die Änderungen.

### Erwartetes Ergebnis (Ausgabe)
Die Änderungen werden übernommen.
Die aktualisierten Werte werden übernommen.

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-03
Speicherung von Änderungen

### Ziel des Tests:
Überprüfung der dauerhaften Speicherung von Änderungen.

### Vorraussetzung 
Änderungen an einem Habit wurden vorgenommen.

### Aktion:
Nutzer schließt die Bearbeitungsansicht.
Nutzer öffnet dasselbe Habit erneut.

### Erwartetes Ergebnis (Ausgabe)
Die zuvor vorgenommenen Änderungen sind weiterhin vorhanden.

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-04
Feedback nach erfolgreicher Änderung

### Ziel des Tests:
Überprüfung, ob der nutzer eine Rückmeldung nach erfolgreicher Speicherung erhält.

### Vorraussetzung 
Änderungen wurden gespeichert.

### Aktion:
Nutzer beobachtet die Oberfläche nach dem Speichern.

### Erwartetes Ergebnis (Ausgabe)
Eine sichtbare Rückmeldung über den erfolgreichen Abschluss wird angezeigt. 

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-05
Löschen eines Habits

### Ziel des Tests:
Überprüfung, ob ein Habit gelöscht werden kann.

### Vorraussetzung 
Nutzer ist eingeloggt. Es existiert mindestens ein Habit. 

### Aktion:
Nutzer löscht ein Habit.

### Erwartetes Ergebnis (Ausgabe)
Das Habit wird nicht merh angezeigt.
Das Habit steht nicht mehr zur Bearbeitung zur Verfügung.

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

---

## Userstory : B033

### Akzeptanztest AT-01
"Erledigte" Habits sind visuell erkennbar.

### Ziel des Tests:
Überprüfung des visuellen Feedbacks nach dem als "erledigt" Markieren des Habits.

### Vorraussetzung 
Ein Habit wurde erfolgreich als "erledigt" markiert.

### Aktion:
Nutzer betrachtet die Habit-Übersicht.

### Erwartetes Ergebnis (Ausgabe)
Das "erledigte" Habit ist visuell eindeutig von nicht erledigten Habits unterscheidbar.

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-02
Korrektur innerhalb desselben Tages.

### Ziel des Tests:
Überprüfung der Möglichkeit zur Korrektur der Markierung am selben Tag.

### Vorraussetzung 
Nutzer befindet sich am selben Kalendertag 
Ein Habit wurde zuvor als erledigt markiert 

### Aktion:
Nutzer hebt die Erledigt-Markierung auf

### Erwartetes Ergebnis (Ausgabe)
Der Habit gilt wieder als offen
Eine erneute Markierung ist möglich

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

---

## Userstory : B065

### Akzeptanztest AT-01
Anzeige des Kalenders auf der Startseite

### Ziel des Tests:
Überprüfung, dass auf der Startseite eine Kalenderansicht angezeigt wird. 

### Vorraussetzung 
Nutzer ist eingeloggt
Die Startseite wird geladen

### Aktion:
Nutzer befindet sich auf der Startseite.

### Erwartetes Ergebnis (Ausgabe)
Auf der Startseite wird ein Kalender angezeigt.

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-02
Strukturierte Darstellung der Kalenderansicht

### Ziel des Tests:
Überprüfung, dass die Kalenderansicht Tage in einer klaren und nachvollziehbaren Struktur darstellt.

### Vorraussetzung 
Nutzer ist eingeloggt
Der Kalender ist sichtbar

### Aktion:
Nutzer betrachtet die Kalenderansicht.

### Erwartetes Ergebnis (Ausgabe)
Tage sind übersichtlich angeordnet   
Die Darstellung entspricht einer klaren Struktur (z. B. Monats- oder Wochenübersicht) 

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-03
 Visualisierung erledigter Habits pro Tag

### Ziel des Tests:
Überprüfung, dass für einzelne Tage visualisiert wird, ob Habits erledigt wurden.

### Vorraussetzung 
Nutzer ist eingeloggt   
Es existieren Tage mit erledigten und nicht erledigten Habits 

### Aktion:
Nutzer betrachtet verschiedene Tage im Kalender

### Erwartetes Ergebnis (Ausgabe)
Tage mit erledigten Habits sind visuell gekennzeichnet.   
Tage ohne erledigte Habits sind entsprechend anders oder neutral dargestellt.

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

---

## Userstory : B023

### Akzeptanztest AT-01
Validierung fehlerhafter Anmeldedaten

### Ziel des Tests:
Überprüfung der Eingabevalidierung bei falschen Zugangsdaten. 

### Vorraussetzung 
Login-Maske ist geöffnet

###Aktion:
Nutzer gibt nicht registrierte Anmeldeinformationen ein.
Nutzer bestätigt die Anmeldung.

### Erwartetes Ergebnis (Ausgabe)
Anmeldung ist nicht erfolgreich
Eine verständliche Fehlermeldung wird angezeigt

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-02
Anzeige ausschließlich eigener Habits nach Login

### Ziel des Tests:
Überprüfung des Zugriffsschutzes auf persönliche Daten.

### Vorraussetzung 
Nutzer ist eingeloggt
Es existieren Habits mehrerer Nutzer im System

### Aktion:
Nutzer öffnet die Habit-Übersicht

### Erwartetes Ergebnis (Ausgabe)
Es werden ausschließlich die eigenen Habits des Nutzers angezeigt.

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

---

## Userstory : B061

=== Akzeptanztest AT-01
Weiterleitung auf die Startseite nach erfolgreicher Anmeldung

### Ziel des Tests:
Überprüfung, dass Nutzer nach erfolgreicher Anmeldung automatisch auf die Startseite weitergeleitet werden.

### Vorraussetzung 
Nutzerkonto existiert 
Nutzer ist nicht eingeloggt

### Aktion:
 Nutzer meldet sich mit gültigen Zugangsdaten an.

### Erwartetes Ergebnis (Ausgabe)
Die Startseite wird unmittelbar nach der Anmeldung angezeigt.

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-02
Zugriff auf die Startseite nur für angemeldete Nutzer.

### Ziel des Tests:
Überprüfung, dass die Startseite ausschließlich für angemeldete Nutzer zugänglich ist.

### Vorraussetzung 
Nutzer ist nicht eingeloggt.

### Aktion:
Nutzer ruft die URL der Startseite direkt auf.

### Erwartetes Ergebnis (Ausgabe)
Der Nutzer wird auf die Login-Seite weitergeleitet

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-03
Anzeige relevanter Habits auf der Startseite.

### Ziel des Tests:
Überprüfung, dass auf der Startseite relevante Habits angezeigt werden.

### Vorraussetzung 
Nutzer ist eingeloggt 
Es existieren zu erledigende und erledigte Habits für den aktuellen Tag

### Aktion:
Nutzer betrachtet die Startseite.

### Erwartetes Ergebnis (Ausgabe)
Zu erledigende Habits werden angezeigt 
Bereits erledigte Habits werden angezeigt

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

## Userstory :B161

### Akzeptanztest AT-01
Sichtbarkeit der Navigationseinträge

### Ziel des Tests:
Überprüfung, dass die wichtigsten Funktionen in der Navigationsbar sichtbar sind.

### Vorraussetzung 
Nutzer ist eingeloggt 
Anwendung ist geladen

### Aktion:
Nutzer betrachtet die Navigationsbar

### Erwartetes Ergebnis (Ausgabe)
Der Reiter "Habit anlegen" ist sichtbar 
Der Reiter "Übersicht" ist sichtbar

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-02
Navigation zu „Habit anlegen“

### Ziel des Tests:
Überprüfung, dass über die Navigationsbar zur Seite „Habit anlegen“ navigiert werden kann.

### Vorraussetzung 
Nutzer ist eingeloggt 
Navigationsbar ist sichtbar

### Aktion:
Nutzer klickt auf den Reiter "Habit anlegen"

### Erwartetes Ergebnis (Ausgabe)
Die Seite „Habit anlegen“ wird geöffnet

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-03
Navigation zur Übersicht der Habits 

### Ziel des Tests:
Überprüfung, dass über die Navigationsbar zur Übersicht der Habits navigiert werden kann.

### Vorraussetzung 
Nutzer ist eingeloggt 
Navigationsbar ist sichtbar

### Aktion:
Nutzer klickt auf den Reiter "Übersicht"

### Erwartetes Ergebnis (Ausgabe)
Die Seite „Übersicht über die Habits“ (Startseite) wird geöffnet

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

## Userstory :B170

### Akzeptanztest AT-01
Konsistentes Farbkonzept wird im gesamten Frontend verwendet

### Ziel des Tests:
Überprüfung, dass ein einheitliches Farbkonzept existiert und durchgängig im Frontend angewendet wird. 

### Vorraussetzung 
Das Frontend ist lauffähig und es existieren mehrere UI-Seiten/Ansichten.
Ein Farbkonzept (z. B. definierte Primär-/Sekundärfarben und Zustandsfarben) ist festgelegt.

### Aktion:
 Relevante Seiten/Ansichten des Frontends werden nacheinander geöffnet und visuell überprüft.

### Erwartetes Ergebnis (Ausgabe)
Farben werden konsistent gemäß dem festgelegten Farbkonzept verwendet.
Es sind keine abweichenden „Ad-hoc“-Farben für vergleichbare Zwecke erkennbar.

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-02
CSS-Klassen folgen einer klaren, einheitlichen Benennungslogik

### Ziel des Tests:
Überprüfung, dass CSS-Klassen einem einheitlichen Benennungsschema folgen.

### Vorraussetzung 
Ein Benennungsschema ist definiert (z. B. BEM oder teaminternes Schema) 
Der Quellcode des Frontends ist zugänglich

### Aktion:
Stichprobenartig werden CSS/SCSS-Dateien sowie Komponenten-Dateien (z. B. React-Komponenten) überprüft.

### Erwartetes Ergebnis (Ausgabe)
CSS-Klassennamen entsprechen dem definierten Schema 
Es sind keine uneinheitlichen oder widersprüchlichen Benennungen erkennbar

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

### Akzeptanztest AT-03
Gleiche UI-Elemente werden visuell gleich dargestellt

### Ziel des Tests:
Überprüfung, dass gleiche UI-Elemente (z. B. Buttons, Inputs, Cards) überall einheitlich aussehen.

### Vorraussetzung 
Es existieren mehrfach verwendete UI-Elemente in verschiedenen Ansichten.
Das Frontend ist lauffähig.

### Aktion:
 In mehreren Ansichten werden gleiche UI-Elemente miteinander verglichen.

### Erwartetes Ergebnis (Ausgabe)
Gleiche UI-Elemente besitzen identische oder klar definierte, konsistente Varianten (z. B. Primary/Secondary).
Abweichungen treten nur auf, wenn sie als Variante im Konzept vorgesehen sind.

### Testergebnis:
- [ ] bestanden
- [ ] nicht bestanden

